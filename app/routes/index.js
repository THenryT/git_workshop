var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/dsfasdf', function(req, reasdfs, next) {
  res.render('index', { title: 'Exasdfpress' });
  res.render('index', { title: 'Exasdfpress' });
});

module.exports = router;
